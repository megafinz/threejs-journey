{
  "name": "03--basic-scene",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "rm -r dist && tsup index.ts --format iife",
    "dev": "rm -r dist && tsup index.ts --format iife --watch"
  },
  "devDependencies": {
    "@types/three": "^0.149.0",
    "tsup": "^6.7.0",
    "typescript": "^5.0.2"
  },
  "dependencies": {
    "three": "^0.150.1"
  }
}
